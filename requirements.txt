# Python environment for notebooks code_0.ipynb – code_8.ipynb
# Install: pip install -r requirements.txt
# Recommended: Python 3.10–3.11. Use a fresh venv/conda env to avoid conflicts.

# --- Core numerics & tables ---
numpy>=1.23,<3
pandas>=2.0,<3
scipy>=1.10
numexpr>=2.10.0

# --- ML & stats ---
scikit-learn>=1.3,<1.7
statsmodels>=0.14.6
tqdm>=4.65

# --- Geospatial (code_0, 1, 2, 3, 4, 6, 7, 8) ---
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyogrio>=0.7

# --- Spatial econometrics / statistics (code_1, 6, 8) ---
# libpysal pulls numba (e.g. Gabriel weights); numba<0.59 raises "Numba needs NumPy 1.24 or less" with NumPy 2.x
numba>=0.59.0
llvmlite>=0.42.0
libpysal>=4.9
esda>=2.5
mgwr>=2.2
spreg>=1.3

# --- Plotting & notebooks ---
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18
ipykernel>=6.0
ipython>=8.0
jupyter

# --- Causal inference (code_4) ---
# statsmodels>=0.14.6: needed for scipy 1.14+ (_lazywhere removed from scipy._lib._util)
# econml>=0.16 + shap>=0.48: NumPy 2.x; shap<0.48 breaks on np.obj2sctype with NumPy 2
econml>=0.16.0
shap>=0.48.0

# --- Sentence embeddings & Hugging Face stack (code_0) ---
sentence-transformers>=3.0
transformers>=4.40
datasets>=2.19
huggingface_hub>=0.26,<2

# --- Quantum optimisation / Ising (code_7) ---
qiskit>=1.0
qiskit-aer>=0.13
qiskit-optimization>=0.6
qiskit-algorithms>=0.3
